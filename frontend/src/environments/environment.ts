export const environment = {
  production: false,
  // Requests are relative; `ng serve` proxies /api and /actuator to the Order Service
  // (see proxy.conf.json). In a real deploy this is set to the API origin.
  apiBase: '',
};
